# ============================================================
# Voice Harness — Requirements
# Python 3.11.x REQUIRED (3.12+ breaks OpenWakeWord/webrtcvad)
#
# INSTALL ORDER MATTERS — use setup/install.py, not pip install -r directly.
# PyTorch (CUDA) must be installed first or CPU-only torch gets locked in.
# ============================================================

# --- UI ---
PyQt6>=6.6.0,<6.8.0
PyQt6-WebEngine>=6.6.0,<6.8.0   # Separate package — easy to miss; included from Phase 1

# --- STT / VAD / Audio ---
# torch and torchaudio are installed FIRST by setup/install.py via CUDA index URL
# Do NOT let pip resolve them here or you get CPU-only builds.
RealtimeSTT>=0.3.104
ctranslate2==4.4.0               # Pin: >=4.5.0 needs cuDNN 9.2 which conflicts on RTX 4080 setup
webrtcvad-wheels>=2.0.14         # Use -wheels variant: has prebuilt Windows wheels for Python 3.11
soundfile>=0.12.0

# --- Wake Word ---
openwakeword>=0.6.0              # Must be >=0.6.0; RealtimeSTT's setup.py floor is too low

# --- LLM ---
ollama>=0.2.0                    # Python client for Ollama REST API (localhost:11434)

# --- TTS ---
kokoro>=0.9.0                    # hexgrad/kokoro — Apache 2.0; requires espeak-ng system binary on Windows

# --- Git integration ---
gitpython>=3.1.40

# --- Code intelligence (Phase 3b) ---
tree-sitter>=0.21.0
